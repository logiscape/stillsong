One-line: the song transport — pill-shaped, glass over the photo, brass play button.

```jsx
<Player playing={playing} position={pos} duration={134} volume={vol}
  onToggle={toggle} onSeek={seek} onVolume={setVol} />
```

Autoplays when arriving from a finished creation; does **not** autoplay when opened from the Sanctuary. No waveform, no EQ, no pro-audio chrome.
