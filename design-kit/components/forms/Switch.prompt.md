One-line: preference toggle with an optional explaining line.

```jsx
<Switch checked={autoScroll} onChange={setAutoScroll}
  label="Gently scroll the lyrics" hint="An estimate — the words aren't timed to the music." />
```
