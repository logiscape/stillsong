One-line: dropdown for short closed lists — lyrics language, MP3 quality.

```jsx
<Select label="Lyrics language" value={lang} onChange={setLang}
  options={["English","Spanish","French","German","Italian","Portuguese","Japanese","Korean","Chinese"]} />
```

Hide the language select entirely when Instrumental is chosen — don't disable it.
