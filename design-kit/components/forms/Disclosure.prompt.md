One-line: hairline-topped collapsible section for optional or technical controls.

```jsx
<Disclosure summary="Add a touch of direction (optional)">…fields…</Disclosure>
```

Anything a first-time user doesn't need belongs inside one. Never nest two levels deep except Remix > Advanced.
