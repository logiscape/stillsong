One-line: the container surface — matte on the app ground, glass or solid over a photo.

```jsx
<Panel>…</Panel>
<Panel variant="glass" style={{maxWidth:"var(--measure-lyric)"}}>…lyrics…</Panel>
```

`glass` and `solid` are the two lyric-column backings from the legibility spec; never put `matte` over a photo.
