One-line: circular icon control for chrome, card players and photo overlays.

```jsx
<IconButton icon="play" label="Play" variant="brass" size={52} />
<IconButton icon="x" label="Close" variant="glass" />
```

Use `glass` over any photo (blurred, hairline), `brass` only for the primary play affordance, `ghost` in panels.
