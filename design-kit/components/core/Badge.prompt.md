One-line: quiet metadata pill — genre tag, duration, "3 versions", instrumental glyph.

```jsx
<Badge>slow folk ballad</Badge>
<Badge mono>2:14</Badge>
<Badge tone="glass" icon="audio-lines">Instrumental</Badge>
```

Use `glass` when the pill sits on a photo. Never use badges as buttons.
