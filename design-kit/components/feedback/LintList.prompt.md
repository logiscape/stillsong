One-line: the Remix lint list — warnings and errors on the caption and lyrics, in plain words.

```jsx
<LintList items={[{level:"warn",message:"This caption is very long — the songwriter may skip the end."}]} />
```

Empty means "Looks good." — never an empty box.
